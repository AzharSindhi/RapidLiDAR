pytorch-lightning==2.6.1
numpy
pyyaml
natsort
click
open3d==0.19.0
wandb==0.27.0
ninja==1.13.0
psutil==7.2.2
huggingface_hub

# mmcv provides the MultiScaleDeformableAttention CUDA op and must be installed
# separately with OpenMMLab's mim (it is not pip-installable with build isolation):
#   pip install -U openmim
#   mim install mmcv==2.2.0
